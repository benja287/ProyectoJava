VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Links en mails y QR: URL pública del sitio (sin barra final). Si no existe, usa el origin del navegador (localhost).
# Poné aquí tu dominio en producción para que los correos no lleven localhost.
VITE_PUBLIC_APP_URL=
