# Frontend (React)

Carpeta reservada para el frontend del congreso.

Por ahora el código React sigue en la raíz del repo (`src/`, `package.json`, etc.).
En una etapa posterior podés moverlo acá:

```bash
# ejemplo futuro
mv src package.json package-lock.json vite.config.ts index.html tsconfig.json frontend/
```

El pipeline actual de GitLab **solo construye y despliega el backend Java** (`/backend`).
